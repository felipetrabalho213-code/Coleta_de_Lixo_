name: segue_coleta
description: "Aplicativo Segue Coleta"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.12.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  table_calendar: ^3.1.2
  flutter_localizations:
    sdk: flutter
  firebase_core: ^4.12.1
  firebase_messaging: ^16.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  # ✅ Caminho das imagens (logo etc)
  assets:
    - assets/images/

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/icone.png" # Caminho para a sua logo
  min_sdk_android: 21    